---
- name: Create group
  group:
    name: "{{ kodi_group }}"
    system: true

- name: Create user
  user:
    name: "{{ kodi_user }}"
    group: "{{ kodi_group }}"
    groups: [ "audio", "input", "video" ]
    system: true
  register: user

- name: Install packages
  apt:
    name:
      - "kodi={{ kodi_version }}"
      - "kodi-bin={{ kodi_version }}"
      - "kodi-audioencoder-flac"
      - "kodi-audioencoder-lame"
      - "kodi-audioencoder-vorbis"
      - "kodi-audioencoder-wav"
      - "kodi-imagedecoder-heif"
      - "kodi-imagedecoder-raw"
      - "kodi-inputstream-adaptive"
      - "kodi-inputstream-rtmp"
      - "kodi-peripheral-joystick"
    update_cache: yes

# https://github.com/xbmc/xbmc/issues/19738
- name: Pin libcurl3-gnutls
  apt:
    name: libcurl3-gnutls=7.64.0-4+deb10u2

- name: Create directory
  file:
    path: "{{ user.home }}/.kodi/userdata"
    owner: "{{ kodi_user }}"
    group: "{{ kodi_group }}"
    mode: "0755"
    state: directory

- name: Configure sources
  template:
    src: sources.xml.j2
    dest: "{{ user.home }}/.kodi/userdata/sources.xml"
  notify:
    - restart

- name: Configure advanced settings
  template:
    src: advancedsettings.xml.j2
    dest: "{{ user.home }}/.kodi/userdata/advancedsettings.xml"
  notify:
    - restart

- name: Check {{ user.home }}/{{ kodi_guisettings_file }}
  stat:
    path: "{{ user.home }}/{{ kodi_guisettings_file }}"
  register: guisettings_file

- name: Configure GUI
  xml:
    file: "{{ user.home }}/{{ kodi_guisettings_file }}"
    xpath: "/settings/setting[@id='{{ item.id }}']"
    value: "{{ item.value }}"
  with_items: "{{ kodi_gui_settings }}"
  when: guisettings_file.stat.exists
  notify:
    - restart

- name: Configure GUI (drop default attributes)
  xml:
    path: "{{ user.home }}/{{ kodi_guisettings_file }}"
    xpath: "/settings/setting[@id='{{ item.id }}']/@default"
    state: absent
  with_items: "{{ kodi_gui_settings }}"
  when: guisettings_file.stat.exists
  notify:
    - restart

- name: Install service
  template:
    src: kodi.service
    dest: /etc/systemd/system/kodi.service
  notify:
    - restart

- name: Enable service
  systemd:
    name: kodi
    state: started
    enabled: yes
